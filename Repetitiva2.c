#include <stdio.h>

Int main() {
    Int n, i = 1, suma = 0;
    Printf("Introduce un número: ");
    Scanf("%d", &n);
    While (i <= n) {
        Suma += i;
        I++;
    }
    Printf("La suma de los primeros %d números es: %d\n", n, suma);
    Return 0;
}
